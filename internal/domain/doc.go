// Package domain holds portable WireHub business rules split by bounded context.
//
// Subpackages: client (WireGuard .conf), forward (port-forward validation),
// hub (hub config import), mapdom (service maps), peer (hostnames, online status),
// policy (group ACL and SNAT), runtime (SyncBundle and DNS catalog).
package domain
