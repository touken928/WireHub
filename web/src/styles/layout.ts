/** Shared layout widths for app and auth screens. */
export const LAYOUT = {
  /** Max width for Dashboard, Groups, Peers, Settings (main shell). */
  pageMaxWidth: '1120px',
  /** Centered card width on Login / Setup. */
  authPanelWidth: '480px',
  /** Vertical gap between page header and main content. */
  pageGap: '16px',
  /** Gap between page title (Subtitle2) and description line. */
  pageHeaderGap: '4px',
} as const;
